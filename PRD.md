# 서점·카페 웹사이트 제품 요구사항 문서 (PRD)

## 1. 문서 개요

| 항목 | 내용 |
| --- | --- |
| 제품명 | Books & Coffee Website (가칭) |
| 문서 상태 | MVP 구현 기준안 |
| 대상 배포 환경 | Vercel Production |
| 디자인 기준 | [`design.md`](./design.md) |
| 주요 사용자 | 매장 방문 예정 고객, 메뉴 탐색 고객, 행사 참가자, 대관 문의자, 지역 작가 |
| 핵심 목적 | 매장의 분위기와 정보를 효과적으로 전달하고 방문·주문·행사 참여·문의 전환을 만든다. |

이 문서는 독립 서점과 카페를 위한 반응형 마케팅 웹사이트의 MVP 요구사항을 정의한다. 참고 사이트의 정보 구조와 편집형 분위기만 참고하며, 로고·사진·문구·메뉴명·행사 이미지 등 브랜드 자산은 모두 독자적으로 제작한다.

## 2. 배경과 문제 정의

사용자는 매장을 방문하기 전에 다음 정보를 빠르게 확인하려 한다.

- 어떤 분위기와 정체성을 가진 공간인지
- 위치, 연락처, 영업시간이 무엇인지
- 대표 메뉴와 온라인 주문 방법이 무엇인지
- 예정된 행사와 참여 방법이 무엇인지
- 대관 또는 지역 작가 입점 문의를 어떻게 보낼 수 있는지

현재 필요한 제품은 이 정보를 한 페이지의 자연스러운 이야기 흐름 안에서 제공하고, 모바일에서도 탐색과 문의가 쉬우며, 별도 서버 운영 부담 없이 Vercel에서 안정적으로 배포 가능한 웹사이트다.

## 3. 제품 목표

### 3.1 핵심 목표

1. 방문자가 첫 화면에서 매장의 성격과 핵심 행동을 5초 이내에 이해한다.
2. 위치·영업시간·연락처·메뉴·행사 정보를 모바일과 데스크톱에서 쉽게 찾을 수 있다.
3. 온라인 주문, 길 찾기, 행사 신청, 대관 문의, 작가 문의로 연결되는 명확한 CTA를 제공한다.
4. 이미지 중심 디자인에도 불구하고 접근성 및 Core Web Vitals 목표를 충족한다.
5. Git 기반으로 변경 사항을 관리하고 Vercel Preview 및 Production 배포가 가능해야 한다.

### 3.2 성공 지표

운영 초기에는 분석 도구가 연결된 시점부터 아래 지표를 측정한다.

| 지표 | MVP 목표 |
| --- | --- |
| 모바일 Lighthouse Performance | 85점 이상 |
| Lighthouse Accessibility | 95점 이상 |
| LCP | 2.5초 이하 |
| CLS | 0.1 이하 |
| INP | 200ms 이하 |
| 주요 CTA 클릭 측정 | 주문, 길 찾기, RSVP, 문의 제출 모두 측정 가능 |
| 폼 제출 성공률 | 유효한 제출 시 95% 이상 |
| 깨진 내부 링크 | 0개 |
| 모바일 주요 화면 가로 스크롤 | 0개 |

분석 서비스가 MVP 출시 시점에 포함되지 않으면 CTA 이벤트 이름과 연결 지점만 코드에 정의하고 실제 수집은 후속 단계에서 활성화한다.

## 4. 범위

### 4.1 MVP 포함 범위

- 반응형 단일 홈 페이지
- 데스크톱 헤더 및 모바일 메뉴
- 공지 바
- 히어로, 브랜드 소개, 위치 및 영업시간
- 대표 메뉴 목록
- 예정 행사 목록 및 빈 상태
- 도서 주문 프로모션 배너
- 대관 문의 폼
- 지역 작가 문의 폼
- 뉴스레터 신청 UI
- 푸터, 법적 링크, 소셜 링크
- SEO 메타데이터, Open Graph, 사이트맵, robots 설정
- 폼 유효성 검사 및 서버 전송 처리
- 기본 스팸 방지
- Vercel Preview/Production 배포 구성
- 오류 페이지와 로딩/제출 상태

### 4.2 MVP 제외 범위

- 자체 결제 및 장바구니
- 자체 온라인 주문 시스템
- 회원가입 및 로그인
- 좌석 예약 시스템
- 실시간 재고 관리
- 관리자 대시보드
- 다국어 전환
- 블로그/저널 상세 CMS
- 행사 티켓 결제
- 실시간 채팅
- 네이티브 모바일 앱

온라인 주문과 행사 신청은 운영자가 제공하는 외부 서비스 URL로 연결한다.

## 5. 가정과 의사결정

- 브랜드명, 실제 주소, 영업시간, 연락처, 주문 URL, 행사 URL, 소셜 URL은 구현 전 운영자가 제공한다.
- 사진과 로고는 독자 제작 또는 적법하게 라이선스된 자산만 사용한다.
- 초기 콘텐츠는 저장소 내 타입이 지정된 정적 데이터로 관리한다. 빈번한 수정이나 비개발자 편집이 필요해지면 CMS 도입을 별도 결정한다.
- 권장 구현은 `Next.js App Router + TypeScript`이며 Vercel의 표준 Git 배포 흐름을 사용한다.
- 스타일 구현 방식은 프로젝트 초기 설정에서 하나만 선택한다. CSS Modules 또는 Tailwind CSS 모두 가능하나 혼용하지 않는다.
- 폼 전송은 서버 측 Route Handler 또는 Server Action을 통해 처리한다. 이메일/데이터 저장 제공자는 환경 변수로 분리하며 공급자는 구현 시작 전에 확정한다.
- 뉴스레터는 제공자가 확정되기 전까지 실제 구독 API 대신 명확한 미연결 상태로 두며, 성공한 것처럼 표시하지 않는다.

## 6. 사용자 유형과 주요 시나리오

### 6.1 첫 방문 고객

> 방문자로서 매장의 분위기, 위치와 영업시간을 확인해 방문 여부를 결정하고 싶다.

완료 조건:

- 히어로에서 공간의 성격을 이해할 수 있다.
- 내비게이션 또는 페이지 흐름을 통해 위치/시간 섹션에 도달할 수 있다.
- 전화, 이메일, 길 찾기 링크를 바로 실행할 수 있다.

### 6.2 메뉴 탐색 및 주문 고객

> 고객으로서 대표 음료를 살펴보고 외부 주문 페이지로 이동하고 싶다.

완료 조건:

- 메뉴 이름, 설명, 이미지, 선택적 가격을 확인할 수 있다.
- `온라인 주문` CTA가 명확하며 새 출처로 이동한다는 것을 예측할 수 있다.

### 6.3 행사 참가자

> 방문자로서 예정된 행사의 날짜와 장소를 확인하고 신청하고 싶다.

완료 조건:

- 행사 날짜, 제목, 장소, 설명을 이미지와 무관하게 읽을 수 있다.
- 각 행사에 유효한 `RSVP` 또는 `자세히 보기` 링크가 있다.
- 행사가 없을 때 빈 화면 대신 뉴스레터 안내가 표시된다.

### 6.4 대관 문의자

> 고객으로서 희망 일시와 인원을 포함한 대관 문의를 보내고 결과를 확인하고 싶다.

완료 조건:

- 필수 항목과 오류 원인을 명확히 알 수 있다.
- 제출 중 중복 제출이 방지된다.
- 성공 또는 실패 결과가 동일한 폼 영역에 표시되고 보조기술에도 전달된다.

### 6.5 지역 작가

> 작가로서 도서 입점 검토에 필요한 정보를 제출하고 싶다.

완료 조건:

- 제출 대상과 필요한 정보를 사전에 확인할 수 있다.
- 이름, 이메일, 도서명/ISBN, 출판사, 정가, 소개 메시지를 제출할 수 있다.

## 7. 정보 구조

```text
/
├─ Header / Navigation
├─ Announcement
├─ Hero
├─ About
├─ Visit (Location & Hours)
├─ Featured Menu
├─ Upcoming Events
├─ Book Order Banner
├─ Private Event Inquiry
├─ Local Author Submission
└─ Newsletter / Footer

/privacy       개인정보처리방침
/accessibility 접근성 안내
/404           찾을 수 없는 페이지
```

홈 내부 탐색은 `#about`, `#visit`, `#menu`, `#events`, `#private-events`, `#local-authors` 앵커를 사용한다. 고정 헤더로 제목이 가려지지 않도록 `scroll-margin-top`을 적용한다.

## 8. 기능 요구사항

요구사항 ID는 구현·테스트·검수에서 동일하게 사용한다.

### 8.1 전역 내비게이션

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| NAV-01 | 헤더에 로고, 홈 내부 섹션 링크, 온라인 주문 CTA를 제공한다. | Must |
| NAV-02 | `900px` 미만에서는 데스크톱 메뉴를 모바일 메뉴로 전환한다. | Must |
| NAV-03 | 모바일 메뉴 열림 시 키보드 포커스를 메뉴 안에서 관리하고 닫은 후 트리거로 반환한다. | Must |
| NAV-04 | ESC, 닫기 버튼, 링크 선택으로 모바일 메뉴를 닫을 수 있다. | Must |
| NAV-05 | 모든 내비게이션 터치 영역은 최소 `44×44px`이다. | Must |

### 8.2 공지 및 히어로

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| HERO-01 | 선택적 공지 문구와 링크를 헤더 아래에 표시한다. | Must |
| HERO-02 | 공지가 없으면 빈 공간 없이 공지 영역을 렌더링하지 않는다. | Must |
| HERO-03 | 히어로에는 하나의 H1, 설명, 배경 이미지, 선택적 CTA를 제공한다. | Must |
| HERO-04 | 이미지 위 텍스트는 모든 지원 화면에서 AA 대비를 확보한다. | Must |

### 8.3 소개 및 방문 정보

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| INFO-01 | 브랜드 소개 문구와 1~2개의 이미지를 제공한다. | Must |
| INFO-02 | 주소, 전화번호, 이메일, 요일별 영업시간을 표시한다. | Must |
| INFO-03 | 전화는 `tel:`, 이메일은 `mailto:`, 길 찾기는 외부 지도 URL로 연결한다. | Must |
| INFO-04 | 휴무일 또는 특별 영업시간을 텍스트로 명확히 표현할 수 있다. | Should |

### 8.4 대표 메뉴

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| MENU-01 | 메뉴 항목은 이름, 설명, 이미지와 선택적 가격을 가진다. | Must |
| MENU-02 | 데스크톱 3열, 태블릿 2열, 모바일 1열을 기본으로 한다. | Must |
| MENU-03 | 온라인 주문 CTA는 설정된 외부 URL로 이동한다. | Must |
| MENU-04 | 메뉴 데이터가 없으면 섹션을 숨기거나 운영 문구가 있는 빈 상태를 표시한다. | Should |

### 8.5 행사

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| EVT-01 | 각 행사에 제목, ISO 날짜/시간, 표시용 날짜, 장소, 설명, 이미지, URL을 제공한다. | Must |
| EVT-02 | 지난 행사는 기본 목록에서 제외하고 가까운 일정순으로 표시한다. | Must |
| EVT-03 | 행사 URL이 있을 때만 RSVP/상세 CTA를 표시한다. | Must |
| EVT-04 | 예정 행사가 없으면 뉴스레터 CTA가 포함된 빈 상태를 표시한다. | Must |

### 8.6 프로모션 배너

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| PROMO-01 | 이미지, 제목, 선택적 설명, 하나의 CTA를 표시한다. | Must |
| PROMO-02 | 설정된 외부 도서 주문 페이지로 연결한다. | Must |

### 8.7 대관 문의 폼

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| FORM-01 | 이름, 이메일, 전화번호, 행사 유형, 희망 날짜, 시간, 인원, 추가 정보를 입력받는다. | Must |
| FORM-02 | 이름, 이메일, 행사 유형, 희망 날짜, 인원을 필수로 검증한다. | Must |
| FORM-03 | 이메일 형식, 미래 날짜, 인원 범위를 클라이언트와 서버 양쪽에서 검증한다. | Must |
| FORM-04 | 제출 중 버튼을 비활성화하고 중복 전송을 방지한다. | Must |
| FORM-05 | 성공·실패 메시지를 `aria-live` 영역에 표시한다. | Must |
| FORM-06 | 서버 오류 시 사용자가 입력한 값을 유지한다. | Must |
| FORM-07 | 성공한 제출을 설정된 수신 채널로 전달한다. | Must |

### 8.8 지역 작가 폼

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| AUTH-01 | 이름, 이메일, 도서명, ISBN, 출판사, 정가, 소개/메시지를 입력받는다. | Must |
| AUTH-02 | 이름, 이메일, 도서명, 메시지를 필수로 검증한다. | Must |
| AUTH-03 | 제출 상태, 오류 처리, 중복 방지는 대관 폼과 동일한 규칙을 따른다. | Must |
| AUTH-04 | 파일 업로드는 MVP에서 제외한다. | Must |

### 8.9 뉴스레터 및 푸터

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| FOOT-01 | 뉴스레터 신청에 이메일과 명시적 동의 UI를 제공한다. | Must |
| FOOT-02 | 실제 구독 제공자가 연결된 경우에만 성공 응답을 표시한다. | Must |
| FOOT-03 | 푸터에 요약 소개, 방문 정보, 영업시간, 내비게이션, 소셜 및 법적 링크를 제공한다. | Must |
| FOOT-04 | 현재 연도를 자동으로 표시한다. | Should |

## 9. 콘텐츠 및 데이터 요구사항

초기에는 콘텐츠를 코드와 분리된 타입 안전 데이터 파일에서 관리한다.

```ts
type ImageAsset = {
  src: string;
  alt: string;
  width: number;
  height: number;
  focalPoint?: `${number}% ${number}%`;
};

type SiteContent = {
  announcement?: { text: string; href?: string };
  hero: { eyebrow?: string; title: string; body: string; image: ImageAsset; cta?: Link };
  intro: { title: string; paragraphs: string[]; images: ImageAsset[] };
  location: {
    address: string;
    phone: string;
    email: string;
    directionsUrl: string;
    hours: Array<{ label: string; value: string }>;
    image: ImageAsset;
  };
  menuItems: Array<{ id: string; name: string; description: string; price?: string; image: ImageAsset }>;
  events: Array<{
    id: string;
    title: string;
    startsAt: string;
    displayDate: string;
    location: string;
    description: string;
    image: ImageAsset;
    href?: string;
  }>;
  bookOrder: { title: string; body?: string; image: ImageAsset; href: string };
};
```

콘텐츠 규칙:

- 참조 사이트 문구를 그대로 사용하지 않는다.
- 이미지에는 의미 있는 대체 텍스트와 고유 크기를 지정한다.
- 날짜 계산에는 ISO 8601 값을 사용하고 사용자에게 보이는 형식은 별도 필드 또는 포매터로 생성한다.
- 외부 링크는 허용된 `https` URL만 콘텐츠에 입력한다.
- 전화번호, 이메일, 주소는 한 곳에서 관리해 중복 불일치를 방지한다.

## 10. 폼 처리 및 보안

### 10.1 서버 처리

- 클라이언트 입력을 신뢰하지 않고 서버에서 스키마 검증을 다시 수행한다.
- 수신 이메일 주소, API 키, 토큰은 Vercel 환경 변수로만 관리한다.
- 비밀 값은 `NEXT_PUBLIC_` 접두사가 붙은 변수에 저장하지 않는다.
- 응답에는 내부 오류, 스택 트레이스, 공급자 응답 원문을 노출하지 않는다.
- 운영 로그에 메시지 본문, 전화번호, 이메일 전체를 불필요하게 기록하지 않는다.

### 10.2 스팸 방지

MVP는 다음 중 최소 두 가지를 적용한다.

- 숨겨진 honeypot 필드
- 최소 작성 시간 검사
- IP 또는 식별자 기반 요청 빈도 제한
- 필요 시 CAPTCHA/Turnstile 도입

스팸 방지는 키보드와 보조기술 사용자의 정상 제출을 막지 않아야 한다.

### 10.3 개인정보

- 폼 바로 아래에 수집 목적과 처리 안내 링크를 제공한다.
- 필요한 정보만 수집하고, 보관 기간은 실제 운영 정책에 맞게 개인정보처리방침에 명시한다.
- 개인정보처리방침이 확정되기 전에는 Production에서 실제 폼 수집을 활성화하지 않는다.

## 11. 비기능 요구사항

### 11.1 반응형

- 모바일 우선으로 구현한다.
- 기준 분기점은 `640px`, `900px`, `1200px`이며 콘텐츠에 따라 미세 조정할 수 있다.
- `320px` 너비부터 대형 데스크톱까지 기능 손실과 가로 스크롤이 없어야 한다.
- 주요 split layout은 `900px` 미만에서 단일 열로 전환한다.

### 11.2 접근성

- WCAG 2.2 AA 수준을 목표로 한다.
- skip link, 시맨틱 landmark, 논리적 제목 구조, 키보드 조작, 명확한 포커스 표시를 제공한다.
- 색상만으로 상태를 전달하지 않는다.
- 모션은 `prefers-reduced-motion` 설정을 존중한다.
- 폼 label, 오류 메시지, 필수 상태, 제출 결과를 보조기술이 인식할 수 있어야 한다.

### 11.3 성능

- 히어로 이미지는 우선 로드하고 그 외 이미지는 지연 로드한다.
- 이미지는 AVIF/WebP와 반응형 크기를 사용하고 레이아웃 크기를 사전에 확보한다.
- 불필요한 클라이언트 컴포넌트와 서드파티 스크립트를 최소화한다.
- 지도는 초기 화면에 임베드하지 않고 외부 길 찾기 링크를 사용한다.
- 폰트는 필요한 굵기만 로드하고 `font-display: swap` 또는 프레임워크 폰트 최적화를 사용한다.

### 11.4 브라우저 지원

- 최신 두 버전의 Chrome, Edge, Firefox, Safari
- 최신 iOS Safari 및 Android Chrome
- JavaScript 오류가 발생해도 주소, 영업시간, 전화번호 등 핵심 정보는 서버 렌더링 결과에서 읽을 수 있어야 한다.

## 12. SEO 및 공유

- 고유한 페이지 title과 meta description을 제공한다.
- canonical URL은 Production 도메인을 기준으로 한다.
- Open Graph 및 X 공유 이미지를 제공한다.
- `sitemap.xml`과 `robots.txt`를 생성한다.
- LocalBusiness 또는 적합한 하위 유형의 구조화 데이터를 실제 운영 정보와 일치하게 제공한다.
- 주소, 전화번호, 영업시간은 화면과 구조화 데이터에서 동일해야 한다.
- Preview 배포는 검색 엔진에 색인되지 않도록 한다.

## 13. 분석 요구사항

분석 도구 연결 시 아래 이벤트 이름을 사용한다.

| 이벤트 | 발생 조건 | 주요 속성 |
| --- | --- | --- |
| `order_click` | 온라인 주문 CTA 선택 | `placement` |
| `directions_click` | 길 찾기 선택 | `placement` |
| `phone_click` | 전화 링크 선택 | `placement` |
| `event_click` | 행사 CTA 선택 | `event_id`, `action` |
| `private_event_submit` | 대관 폼 서버 성공 | 제출 내용 제외 |
| `author_submit` | 작가 폼 서버 성공 | 제출 내용 제외 |
| `newsletter_submit` | 구독 API 성공 | 이메일 제외 |

개인정보와 자유 입력 폼 내용은 분석 속성으로 전송하지 않는다.

## 14. 권장 기술 구조

```text
app/
├─ layout.tsx
├─ page.tsx
├─ privacy/page.tsx
├─ accessibility/page.tsx
├─ api/
│  ├─ private-events/route.ts
│  ├─ local-authors/route.ts
│  └─ newsletter/route.ts
├─ sitemap.ts
└─ robots.ts
components/
├─ layout/
├─ sections/
├─ forms/
└─ ui/
content/
└─ site.ts
lib/
├─ validation.ts
├─ submissions.ts
└─ analytics.ts
public/
└─ images/
```

구조 원칙:

- 기본은 Server Component로 구현하고 메뉴 열기, 폼 상태 등 상호작용이 필요한 부분만 Client Component로 둔다.
- 반복되는 메뉴와 행사 카드는 데이터 기반으로 렌더링한다.
- 단일 사용 코드를 과도하게 추상화하지 않는다.
- 공급자별 폼 전송 코드는 `lib/submissions.ts` 경계 안에 둔다.

## 15. Vercel 배포 요구사항

### 15.1 환경

| 환경 | 목적 | 요구사항 |
| --- | --- | --- |
| Local | 개발 및 단위 검증 | `.env.local`, 실제 비밀 값 커밋 금지 |
| Preview | PR별 QA | 테스트 수신처 사용, 검색 색인 차단 |
| Production | 실제 사용자 서비스 | 운영 도메인, 운영 수신처, 분석 활성화 |

### 15.2 필수 구성

- Git 저장소를 Vercel 프로젝트에 연결한다.
- Production 브랜치를 명시한다.
- PR마다 Preview Deployment가 생성되어야 한다.
- 필요한 환경 변수는 Development, Preview, Production 범위에 맞게 각각 설정한다.
- 커스텀 도메인을 연결하고 HTTPS가 정상 적용되어야 한다.
- 빌드 명령은 프로젝트 기본값을 사용하고, 배포 전에 lint/typecheck/test/build가 통과해야 한다.
- Production 배포 실패 시 이전 정상 배포를 유지하고 원인 수정 후 재배포한다.

### 15.3 환경 변수 예시

실제 공급자 확정 후 이름을 조정할 수 있다.

```text
SITE_URL=
FORM_PROVIDER_API_KEY=
PRIVATE_EVENT_RECIPIENT=
AUTHOR_SUBMISSION_RECIPIENT=
NEWSLETTER_PROVIDER_API_KEY=
NEWSLETTER_AUDIENCE_ID=
NEXT_PUBLIC_ANALYTICS_ID=
```

예시 파일에는 빈 값만 두며 실제 키는 Git에 커밋하지 않는다.

### 15.4 배포 전 체크

- Production 도메인 기반 canonical, sitemap, OG URL 확인
- Preview와 Production의 폼 수신처 분리 확인
- 모든 환경 변수 존재 여부 확인
- 404, 폼 오류, 외부 링크 실패 상황 확인
- 모바일/데스크톱 시각 회귀 확인
- Lighthouse 및 접근성 검사
- 실제 기기에서 전화, 이메일, 지도, 주문 링크 확인
- 개인정보처리방침과 동의 문구 확인

## 16. 테스트 전략

### 16.1 정적 검증

- TypeScript typecheck
- ESLint
- Production build
- 내부 링크 및 이미지 경로 확인

### 16.2 자동화 테스트

- 날짜 기준 행사 필터링 및 정렬 단위 테스트
- 두 문의 폼의 서버 유효성 검사 단위 테스트
- 정상 제출, 유효성 실패, 제공자 오류 통합 테스트
- 모바일 메뉴 열기/닫기와 키보드 포커스 테스트
- 주요 CTA와 앵커 이동 E2E 테스트

### 16.3 수동 QA 화면

- `320×568`
- `390×844`
- `768×1024`
- `1440×900`
- 키보드 전용 탐색
- 200% 확대
- 모션 감소 설정
- 느린 네트워크에서 이미지 로딩 및 layout shift

## 17. 완료 정의 (Definition of Done)

MVP는 다음 조건을 모두 만족할 때 완료로 간주한다.

- [ ] `design.md`의 섹션 구조와 디자인 토큰이 구현되었다.
- [ ] 브랜드명, 로고, 카피, 사진이 독자적인 자산으로 교체되었다.
- [ ] NAV, HERO, INFO, MENU, EVT, PROMO, FORM, AUTH, FOOT의 Must 요구사항이 충족되었다.
- [ ] 폼의 서버 검증, 스팸 방지, 성공/오류 상태가 동작한다.
- [ ] 개인정보처리방침과 접근성 안내 페이지가 연결되어 있다.
- [ ] 모바일과 데스크톱에서 핵심 사용자 시나리오를 완료할 수 있다.
- [ ] typecheck, lint, 자동화 테스트, Production build가 통과한다.
- [ ] 접근성 및 성능 목표를 충족하거나 예외가 문서화되어 승인되었다.
- [ ] Vercel Preview에서 검수 후 Production 도메인에 배포되었다.
- [ ] Production의 주문, 지도, 전화, 이메일, 행사, 폼 링크를 실제로 검증했다.

## 18. 구현 단계

### Phase 1 — 기반

- Next.js/TypeScript 프로젝트 구성
- 전역 토큰, 폰트, 레이아웃, 콘텐츠 모델
- 이미지와 브랜드 자산 준비

검증: typecheck, lint, 기본 Production build 통과.

### Phase 2 — 콘텐츠 섹션

- 헤더, 공지, 히어로, 소개, 위치, 메뉴, 행사, 프로모션, 푸터
- 반응형 레이아웃과 접근성 적용

검증: 지원 화면 수동 QA, 키보드 탐색, 앵커 및 외부 링크 확인.

### Phase 3 — 폼 및 서비스 연결

- 대관/작가/뉴스레터 폼
- 서버 검증, 전송 제공자, 스팸 방지, 개인정보 안내

검증: 성공, 유효성 실패, 공급자 오류, 중복 제출 테스트.

### Phase 4 — 품질 및 배포

- SEO, 구조화 데이터, 성능 최적화, 분석 이벤트
- Vercel Preview QA, 환경 변수, 도메인 연결, Production 배포

검증: 완료 정의 체크리스트 전체 통과.

## 19. 출시 전 확정이 필요한 항목

아래 항목은 디자인·구현을 진행할 수는 있으나 Production 출시 전에 반드시 확정해야 한다.

1. 최종 브랜드명, 로고, 한/영문 표기
2. 실제 주소, 연락처, 영업시간과 특별 영업시간 정책
3. 온라인 주문 및 도서 주문 외부 URL
4. 행사 신청 URL과 행사 운영 방식
5. 대관/작가 문의 수신 방식 및 담당 주소
6. 뉴스레터 제공자와 동의 문구
7. 개인정보 보관 기간과 삭제 요청 절차
8. 분석 도구 사용 여부와 쿠키 동의 필요 여부
9. Production 도메인과 Git Production 브랜치
10. 최종 사진, 라이선스, 대체 텍스트 승인

## 20. 향후 확장 후보

- Headless CMS를 통한 메뉴·행사·공지 편집
- 행사 상세 페이지와 RSVP 통합
- 메뉴 카테고리 및 필터
- 뉴스/저널 콘텐츠
- 다국어 지원
- 영업시간 예외 일정 자동화
- 관리자용 제출 조회 및 상태 관리

확장 기능은 MVP 사용 데이터와 운영 빈도를 확인한 뒤 별도 요구사항으로 정의한다.
